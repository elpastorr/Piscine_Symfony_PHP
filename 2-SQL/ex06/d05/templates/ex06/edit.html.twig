<!DOCTYPE html>
<html>
<head>
    <title>Users Table</title>
</head>
<body>
    <h1>Edit User</h1>

    <form method="POST">
        Username:
        <input type="text" name="username" value="{{ user.username }}"><br>

        Name:
        <input type="text" name="name" value="{{ user.name }}"><br>

        Email:
        <input type="text" name="email" value="{{ user.email }}"><br>

        Enable:
        <input type="checkbox" name="enable" {% if user.enable %}checked{% endif %}><br>

        Birthdate:
        <input type="datetime-local" name="birthdate"
            value="{{ user.birthdate|date('Y-m-d\\TH:i') }}"><br>

        Address:
        <textarea name="address">{{ user.address }}</textarea><br>

        <button type="submit">Update</button>
    </form>
</body>
</html>