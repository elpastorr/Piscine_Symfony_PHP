<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Ex05 - Delete Users (ORM)</title>
</head>
<body>

<h1>Users</h1>

{% if message %}
    <p><strong>{{ message }}</strong></p>
{% endif %}

<table border="1">
<tr>
    <th>ID</th>
    <th>Username</th>
    <th>Email</th>
    <th>Enabled</th>
    <th>Action</th>
</tr>

{% for user in users %}
<tr>
    <td>{{ user.id }}</td>
    <td>{{ user.username }}</td>
    <td>{{ user.email }}</td>
    <td>{{ user.enable ? 'Yes' : 'No' }}</td>
    <td>
        <a href="{{ path('ex05_delete', {'id': user.id}) }}"
           onclick="return confirm('Are you sure you want to delete this user?');">
           Delete
        </a>
    </td>
</tr>
{% endfor %}
</table>

</body>
</html>